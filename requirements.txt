# Python deps for the Vercel Function in api/ (the scraper). Vercel installs this file automatically.
fastapi>=0.115
httpx>=0.27
# Optional, local only (JS-rendered fallback, set USE_PLAYWRIGHT=1). It cannot run on Vercel.
# playwright>=1.45
